interface:
  display_name: "Bun Runtime"
  short_description: "Choose or debug Bun against a pinned project version."
  default_prompt: "Use $bun-runtime to evaluate or debug Bun for this repository's pinned runtime and toolchain."
